import Loading from "../components/Loading";
// -import styles from "../styles/Home.module.sass";

const Home = () => {
	return (
		<main>
			<Loading show={false} />
		</main>
	);
};

export default Home;
